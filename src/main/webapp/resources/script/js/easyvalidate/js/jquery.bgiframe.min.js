/* Copyright (c) 2010 Brandon Aaron (http://brandonaaron.net)
 * Licensed under the MIT License (LICENSE.txt).
 * $.browser在jQuery1.9里被删除了，所以项目的js代码里用到$.browser的地方就会报这个错。

jQuery1.9里对不少API做了废弃或者删除。幸运的是，jQuery社区考虑到了这么做给开发人员带来的麻烦，
在1.9同时也推出了一个新插件jQuery Migration，该插件可以检查出在1.9之后被废弃或者删除的API，恢复原有功能，
从而保证已有的js代码能在最新的jQuery下正常运行。

下面是具体的解决方法，只需要在jQuery引用后加上一行对jQuery Migration的引用即可。

<script src="http://code.jquery.com/jquery-1.9.1.js"></script>
<script src="http://code.jquery.com/jquery-migrate-1.1.1.js"></script> 
 * Version 2.1.2
 */

(function(a) {
//	a.fn.bgiframe = (a.browser.msie && /msie 6\.0/i.test(navigator.userAgent) ? function(
	a.fn.bgiframe = (a.support.boxModel && /msie 6\.0/i.test(navigator.userAgent) ? function(
			d) {
		d = a.extend({
			top : "auto",
			left : "auto",
			width : "auto",
			height : "auto",
			opacity : true,
			src : "javascript:false;"
		}, d);
		var c = '<iframe class="bgiframe"frameborder="0"tabindex="-1"src="'
				+ d.src
				+ '"style="display:block;position:absolute;z-index:-1;'
				+ (d.opacity !== false ? "filter:Alpha(Opacity='0');" : "")
				+ "top:"
				+ (d.top == "auto" ? "expression(((parseInt(this.parentNode.currentStyle.borderTopWidth)||0)*-1)+'px')"
						: b(d.top))
				+ ";left:"
				+ (d.left == "auto" ? "expression(((parseInt(this.parentNode.currentStyle.borderLeftWidth)||0)*-1)+'px')"
						: b(d.left))
				+ ";width:"
				+ (d.width == "auto" ? "expression(this.parentNode.offsetWidth+'px')"
						: b(d.width))
				+ ";height:"
				+ (d.height == "auto" ? "expression(this.parentNode.offsetHeight+'px')"
						: b(d.height)) + ';"/>';
		return this.each(function() {
			if (a(this).children("iframe.bgiframe").length === 0) {
				this.insertBefore(document.createElement(c), this.firstChild)
			}
		})
	}
			: function() {
				return this
			});
	a.fn.bgIframe = a.fn.bgiframe;
	function b(c) {
		return c && c.constructor === Number ? c + "px" : c
	}
})(jQuery);